<Window x:Class="Library.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Library"
        mc:Ignorable="d"
        Title="Library" Height="600" Width="800"
        Background="#333333"
        >
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TabControl Background="#333333" Grid.Row="0" Grid.Column="0">
            <TabControl.Resources>
                <Style TargetType="TabItem">
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="TabItem">
                                <Grid Name="Panel">
                                    <ContentPresenter x:Name="ContentSite"
                                        VerticalAlignment="Center"
                                        HorizontalAlignment="Center"
                                        ContentSource="Header"
                                        Margin="10,2"/>
                                </Grid>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter TargetName="Panel" Property="Background" Value="LightSkyBlue" />
                                    </Trigger>
                                    <Trigger Property="IsSelected" Value="False">
                                        <Setter TargetName="Panel" Property="Background" Value="White" />
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </TabControl.Resources>
            <TabItem Name="BooksTab" Header="Books" Background="#333333">

                <DockPanel>
                    <DataGrid x:Name="BooksDataGrid" Loaded="Load_BookData">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="IdentificationNumber" Binding="{Binding bookID}"></DataGridTextColumn>
                            <DataGridTextColumn Header="Author" Binding="{Binding bookAuthor}"></DataGridTextColumn>
                            <DataGridTextColumn Header="Title" Binding="{Binding bookTitle}"></DataGridTextColumn>
                            <DataGridTextColumn Header="Published" Binding="{Binding bookPublished}"></DataGridTextColumn>
                            <DataGridTextColumn Header="Publisher" Binding="{Binding bookPublisher}"></DataGridTextColumn>
                            <DataGridCheckBoxColumn Header="Igen" Binding="{Binding bookBoolean}"></DataGridCheckBoxColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </DockPanel>
            </TabItem>
            <TabItem Name="MembersTab" Header="Members" Loaded="Load_MembersData">
                <DockPanel>
                    <DataGrid x:Name="MembersDataGrid">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="MemberID" Binding="{Binding memberID}"></DataGridTextColumn>
                            <DataGridTextColumn Header="Name" Binding="{Binding name}"></DataGridTextColumn>
                            <DataGridTextColumn Header="BirthDate" Binding="{Binding birthDate}"></DataGridTextColumn>
                            <DataGridTextColumn Header="ZipCode" Binding="{Binding zipCode}"></DataGridTextColumn>
                            <DataGridTextColumn Header="City" Binding="{Binding city}"></DataGridTextColumn>
                            <DataGridTextColumn Header="StreetAddress" Binding="{Binding streetAddress}"></DataGridTextColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </DockPanel>
            </TabItem>
            <TabItem Name="RentalsTab" Header="Rental" Loaded="Load_RentalsData">
                <DockPanel>
                    <DataGrid x:Name="RentalsDataGrid">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="KölcsönzésId" Binding="{Binding rentalID}"></DataGridTextColumn>
                            <DataGridTextColumn Header="Olvasod" Binding="{Binding memberID}"></DataGridTextColumn>
                            <DataGridTextColumn Header="KönyvId" Binding="{Binding bookID}"></DataGridTextColumn>
                            <DataGridTextColumn Header="Kölcsönzés dátuma" Binding="{Binding rentalDateStart}"></DataGridTextColumn>
                            <DataGridTextColumn Header="Visszavétel dátuma" Binding="{Binding rentalDateEnd}"></DataGridTextColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </DockPanel>
            </TabItem>
        </TabControl>

    </Grid>
</Window>
